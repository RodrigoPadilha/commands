
sudo chmod +x gradlew

rm -rf node_modules yarn.lock package-lock.json
yarn cache clean
yarn install
yarn start --reset-cache

# Limpar o gradlew cache
cd android && bash gradlew clean

# Limpar o gradlew cache HARD
rm -rf android/.gradle
rm -rf .gradle
rm -rf ~/.gradle
cd android && bash gradlew clean